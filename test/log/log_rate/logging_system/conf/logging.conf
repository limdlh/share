path=./log
level=log
maxfilelen=2
outputtype=w


/*
    "maxfilelen" unit is MB, so: 1 is 1MB size
    "level" could be: fatal, error, warn, info, debug, log or all
    "outputtype" could be: NO_OUTPUT(N/n), WRITE_FILE(W/w), STD_OUT(S/s)
*/
